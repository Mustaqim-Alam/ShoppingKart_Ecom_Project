export interface User {
  name: string;
  email: string;
  dob: string;
  gender: string;
  photo: string;
  role: string;
  _id: string;
}
export interface Product {
  name: string;
  category: string;
  photo: string;
  _id: string;
  stock: number;
  price: number;
}
